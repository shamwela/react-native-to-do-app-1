import { StyleSheet } from 'react-native'

const textColor = 'white'

export const globalStyles = StyleSheet.create({
  heading1: {
    fontSize: 32,
    fontWeight: '700',
    color: textColor,
  },
  regularText: {
    color: textColor,
  },
})
